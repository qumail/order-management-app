{
  "name": "order-management-backend",
  "version": "1.0.0",
  "description": "Backend for order management system",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "cross-env NODE_ENV=test jest --forceExit --detectOpenHandles --runInBand",
    "test:watch": "cross-env NODE_ENV=test jest --watch --forceExit --detectOpenHandles --runInBand",
    "test:coverage": "cross-env NODE_ENV=test jest --coverage --forceExit --detectOpenHandles --runInBand"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-validator": "^7.0.1",
    "mongoose": "^7.5.0",
    "socket.io": "^4.7.2"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "jest": "^29.7.0",
    "mongodb-memory-server": "^11.0.1",
    "nodemon": "^3.0.1",
    "supertest": "^6.3.3"
  }
}
